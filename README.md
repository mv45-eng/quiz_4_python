1. La diferencia entre print() y return es que print() solo imprime la respuesta o el valor dentro de la función (None), y return lo guarda para poder ocuparlo cuando quieras cuantas veces quieras. Respuesta B

2. A, 50 porque el valor porque el valor por DEFAULT es 10, entonces 5*10= 50 

3. 

4. b, from math import sqrt

 5. b) Porque importa todas las funciones y puede causar conflictos de nombres con tu
código

6. b) Las estándar ya vienen con Python, las externas se instalan con pip install

SECCIÓN 2

